<SafeAreaView style={styles.root}>
  <View style={styles.button}>
    <Button title="OS별 로그인" onPress={osLogin}color="black"/>
  </View>
  <View style={styles.button1}>
    <Button title="로그인 없이 계속하기" onPress={simpleLogin} color="black"/>
  </View>
</SafeAreaView>

const styles = StyleSheet.create({
  root: {
    alignItems:'center'
  },
  osButton:{
    borderWidth:1,
    borderRadius:20,
    paddingRight:80,
    paddingLeft:80,
    marginTop:600,
  },
  simpleButton:{
    marginTop:30,
  },
});
